#disableAntiXss()
## A combined css stylesheet is returned with the default styles inlined in one file.

## FIXME: Manually requiring content-styles, because it doesn't seem to get included
## just by the graces of master-style's defined dependency on it...
$!webResourceManager.getResourceContent("confluence.web.resources:content-styles")
$!webResourceManager.getResourceContent("confluence.web.resources:master-styles")

## Some overrides
.page-metadata {
    margin: 0;
}

.page-metadata p {
    margin: 0;
    font-size: 1em;
}

#footer p {
    margin: 0;
    padding: 1em 0;
}

#parse("/styles/colors-css.vm")

$!webResourceManager.getResourceContent("com.atlassian.confluence.themes.default:styles")
